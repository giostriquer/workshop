# Decision: park `vigil`

**Date:** 2026-08-11

Operator call in the same restructuring pass as the onboarding-plugin deletion
and the `workbench` split: `vigil` (advisory governance review of the
agent/skill/workflow layer) leaves both the `toolkit` payload and the repo's own
working set. Spec preserved at `attic/agents/vigil.md`; doc at
`docs/agents/deprecated/vigil.md`; toolkit is five agents; "governance" dropped
from manifest wording; the vigil-specific reviewer-session sections in
`AGENTS.md` collapsed to a generic pointer at the conventions. Promotable back
through the normal inclusion bar if governance review earns its keep again.
